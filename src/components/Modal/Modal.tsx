import {Alert} from 'react-native';
import {Modalize} from 'react-native-modalize';

interface IModalConsultaLote {
  open: boolean;
  onClose: () => void;
}

function ModalScreen() {
  return <></>;
}

export default ModalScreen;
